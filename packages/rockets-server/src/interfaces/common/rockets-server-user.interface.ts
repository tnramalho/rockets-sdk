import { AuthenticatedUserInterface } from '@concepta/nestjs-common';

/**
 * Authenticated User Interface
 *
 * Extends the base authenticated user interface from the common module
 */
export interface RocketsServerUserInterface
  extends AuthenticatedUserInterface {}
