import { AuthRecoveryRecoverLoginDto } from '@concepta/nestjs-auth-recovery';

/**
 * Rockets Server Recover Login DTO
 *
 * Extends the base recovery recover login DTO from the auth-recovery module
 */
export class RocketsServerRecoverLoginDto extends AuthRecoveryRecoverLoginDto {
  /**
   * When extending the base DTO, you can add additional properties
   * specific to your application here
   */
}
